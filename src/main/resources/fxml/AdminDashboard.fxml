<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<AnchorPane xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="com.hero.finalsmartstudentportal.controller.AdminDashboardController"
            prefWidth="1200" prefHeight="700"
            style="-fx-background-color: #f5f6fa;">

    <!-- Top Header -->
    <HBox layoutX="0" layoutY="0"
          prefWidth="1200" prefHeight="60"
          style="-fx-background-color: #2c3e50; -fx-padding: 15;">
        <Label text="Smart Student Portal of Hero - Admin Dashboard"
               style="-fx-text-fill: white; -fx-font-size: 20px; -fx-font-weight: bold;" />
    </HBox>

    <!-- Sidebar -->
    <VBox layoutX="0" layoutY="60"
          prefWidth="220" prefHeight="640"
          spacing="10"
          style="-fx-background-color: #ecf0f1; -fx-padding: 15;">

        <Button fx:id="btnDashboard" text="🏠 Dashboard" onAction="#loadDashboard" prefWidth="190"/>
        <Button fx:id="btnStudents" text="👨‍🎓 Students" onAction="#loadStudents" prefWidth="190"/>
        <Button fx:id="btnCourses" text="📚 Courses" onAction="#loadCourses" prefWidth="190"/>
        <Button fx:id="btnResults" text="📝 Results" onAction="#loadResults" prefWidth="190"/>
        <Button fx:id="btnFees" text="💳 Fees" onAction="#loadFees" prefWidth="190"/>
        <Button fx:id="btnNotices" text="📢 Notices" onAction="#loadNotices" prefWidth="190"/>
        <Button fx:id="btnSettings" text="⚙ Settings" onAction="#loadSettings" prefWidth="190"/>
        <Button fx:id="btnLogout" text="🚪 Logout" onAction="#handleLogout" prefWidth="190"/>

    </VBox>

    <!-- Main Content Area -->
    <VBox fx:id="contentArea"
          layoutX="240" layoutY="60"
          spacing="15"
          style="-fx-padding: 15; -fx-background-color: #ffffff; -fx-border-color: #dcdde1; -fx-border-width: 1; -fx-border-radius: 5;">

        <Label fx:id="lblWelcome" text="Welcome Admin 👋"
               style="-fx-font-size: 26px; -fx-font-weight: bold;" />

        <Label text="Select an option from the left menu"
               style="-fx-font-size: 14px;" />

    </VBox>

</AnchorPane>
